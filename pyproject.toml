[project]
name = "shiv-sagar-voice-agent"
version = "1.0.0"
requires-python = ">=3.9"
dependencies = []

[tool.vercel]
entrypoint = "app:app"

[tool.vercel.scripts]
build = "bash scripts/vercel-build.sh"
